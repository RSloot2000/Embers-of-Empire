#################
##### Debug #####
#################

form_illyricum_pannoniae_fast_decision_effect = {
	every_county_in_region = {
		region = illyricum_pannoniae_diocese_region
		county = { add_to_list = illyricum_pannoniae_diocese_region_list }
	}
	every_in_list = {
		list = illyricum_pannoniae_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_italia_suburbicariae_fast_decision_effect = {
	every_county_in_region = {
		region = italia_suburbicariae_diocese_region
		county = { add_to_list = italia_suburbicariae_diocese_region_list }
	}
	every_in_list = {
		list = italia_suburbicariae_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_italia_annonariae_fast_decision_effect = {
	every_county_in_region = {
		region = italia_annonariae_diocese_region
		county = { add_to_list = italia_annonariae_diocese_region_list }
	}
	every_in_list = {
		list = italia_annonariae_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_hispania_fast_decision_effect = {
	every_county_in_region = {
		region = hispania_diocese_region
		county = { add_to_list = hispania_diocese_region_list }
	}
	every_in_list = {
		list = hispania_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_septem_provinciae_fast_decision_effect = {
	every_county_in_region = {
		region = septem_provinciae_diocese_region
		county = { add_to_list = septem_provinciae_diocese_region_list }
	}
	every_in_list = {
		list = septem_provinciae_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_gallia_fast_decision_effect  = {
	every_county_in_region = {
		region = gallia_diocese_region
		county = { add_to_list = gallia_diocese_region_list }
	}
	every_in_list = {
		list = gallia_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_britannia_fast_decision_effect = {
	every_county_in_region = {
		region = britannia_diocese_region
		county = { add_to_list = britannia_diocese_region_list }
	}
	every_in_list = {
		list = britannia_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_africae_fast_decision_effect = {
	every_county_in_region = {
		region = africae_diocese_region
		county = { add_to_list = africae_diocese_region_list }
	}
	every_in_list = {
		list = africae_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_orientis_fast_decision_effect = {
	every_county_in_region = {
		region = orientis_diocese_region
		county = { add_to_list = orientis_diocese_region_list }
	}
	every_in_list = {
		list = orientis_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_aegyptus_fast_decision_effect = {
	every_county_in_region = {
		region = aegyptus_diocese_region
		county = { add_to_list = aegyptus_diocese_region_list }
	}
	every_in_list = {
		list = aegyptus_diocese_region_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}

form_roman_empire_fast_decision_effect = {
	every_county_in_region = {
		region = custom_better_roman_full_borders
		county = { add_to_list = custom_better_roman_full_borders_list }
	}
	every_in_list = {
		list = custom_better_roman_full_borders_list
		limit = {
			NOT = { holder = root }
		}
		create_title_and_vassal_change = {
            type = usurped
            save_scope_as = change
            add_claim_on_loss = no
        }
		change_title_holder_include_vassals = {
			holder = root
			change = scope:change
		}
		resolve_title_and_vassal_change = scope:change	
	}
}